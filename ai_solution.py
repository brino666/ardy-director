```json
{
  "solution_code": "# ardy_to_unreal/converter.py\n\"\"\"\nARDY to Unreal Engine motion exporter.\n\nConverts ARDY director .npz files to FBX for import into UE5,\nwith proper skeleton mapping for IK Retargeting.\n\"\"\"\n\nimport numpy as np\nimport argparse\nfrom pathlib import Path\nfrom typing import Dict, Any, Optional\nimport logging\n\ntry:\n    import bpy\n    HAS_BLENDER = True\nexcept ImportError:\n    HAS_BLENDER = False\n\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\n# ARDY core skeleton definition (27 joints, Mixamo-style naming)\n# Maps ARDY joint indices to bone names expected by UE5 IK Retargeter\nCORE_SKELETON = [\n    \"Hips\",           # 0\n    \"Spine\",          # 1\n    \"Spine1\",         # 2\n    \"Spine2\",         # 3\n    \"Neck\",           # 4\n    \"Head\",           # 5\n    \"LeftShoulder\",   # 6\n    \"LeftArm\",        # 7\n    \"LeftForeArm\",    # 8\n    \"LeftHand\",       # 9\n    \"RightShoulder\",  # 10\n    \"RightArm\",       # 11\n    \"RightForeArm\",   # 12\n    \"RightHand\",      # 13\n    \"LeftUpLeg\",      # 14\n    \"LeftLeg\",        # 15\n    \"LeftFoot\",       # 16\n    \"LeftToeBase\",    # 17\n    \"RightUpLeg\",     # 18\n    \"RightLeg\",       # 19\n    \"RightFoot\",      # 20\n    \"RightToeBase\",   # 21\n    # Additional joints for fingers/hands if present\n    \"LeftHandThumb1\",  # 22\n    \"LeftHandIndex1\",  # 23\n    \"RightHandThumb1\", # 24\n    \"RightHandIndex1\", # 25\n    \"HeadTop_End\",     # 26\n]\n\n# G1 skeleton (if different, adjust as needed)\nG1_SKELETON = CORE_SKELETON.copy()  # For now, same as core\n\n# Parent relationships for hierarchy (index in list points to parent index)\nCORE_HIERARCHY = {\n    0: -1,   # Hips (root)\n    1: 0,    # Spine -> Hips\n    2: 1,    # Spine1 -> Spine\n    3: 2,    # Spine2 -> Spine1\n    4: 3,    # Neck -> Spine2\n    5: 4,    # Head -> Neck\n    6: 3,    # LeftShoulder -> Spine2\n    7: 6,    # LeftArm -> LeftShoulder\n    8: 7,    # LeftForeArm -> LeftArm\n    9: 8,    # LeftHand -> LeftForeArm\n    10: 3,   # RightShoulder -> Spine2\n    11: 10,  # RightArm -> RightShoulder\n    12: 11,  # RightForeArm -> RightArm\n    13: 12,  # RightHand -> RightForeArm\n    14: 0,   # LeftUpLeg -> Hips\n    15: 14,  # LeftLeg -> LeftUpLeg\n    16: 15,  # LeftFoot -> LeftLeg\n    17: 16,  # LeftToeBase -> LeftFoot\n    18: 0,   # RightUpLeg -> Hips\n    19: 18,  # RightLeg -> RightUpLeg\n    20: 19,  # RightFoot -> RightLeg\n    21: 20,  # RightToeBase -> RightFoot\n    22: 9,   # LeftHandThumb1 -> LeftHand\n    23: 9,   # LeftHandIndex1 -> LeftHand\n    24: 13,  # RightHandThumb1 -> RightHand\n    25: 13,  # RightHandIndex1 -> RightHand\n    26: 5,   # HeadTop_End -> Head\n}\n\n\ndef load_ardy_npz(npz_path: Path) -> Dict[str, Any]:\n    \"\"\"\n    Load ARDY director .npz file.\n    \n    Expected keys:\n    - global_rot_mats: (num_frames, num_joints, 3, 3) rotation matrices\n    - posed_joints: (num_frames, num_joints, 3) joint positions\n    - root_positions: (num_frames, 3) root trajectory\n    - fps: scalar, frames per second\n    \"\"\"\n    logger.info(f\"Loading ARDY motion from {npz_path}\")\n    data = np.load(npz_path)\n    \n    required_keys = ['global_rot_mats', 'posed_joints', 'root_positions', 'fps']\n    for key in required_keys:\n        if key not in data:\n            raise ValueError(f\"Missing required key '{key}' in .npz file\")\n    \n    return {\n        'global_rot_mats': data['global_rot_mats'],\n        'posed_joints': data['posed_joints'],\n        'root_positions': data['root_positions'],\n        'fps': float(data['fps']),\n        'num_frames': data['posed_joints'].shape[0],\n        'num_joints': data['posed_joints'].shape[1],\n    }\n\n\ndef rotation_matrix_to_quaternion(rot_mat: np.ndarray) -> np.ndarray:\n    \"\"\"\n    Convert 3x3 rotation matrix to quaternion (w, x, y, z).\n    \"\"\"\n    trace = np.trace(rot_mat)\n    \n    if trace > 0:\n        s = 0.5 / np.sqrt(trace + 1.0)\n        w = 0.25 / s\n        x = (rot_mat[2, 1] - rot_mat[1, 2]) * s\n        y = (rot_mat[0, 2] - rot_mat[2, 0]) * s\n        z = (rot_mat[1, 0] - rot_mat[0, 1]) * s\n    elif rot_mat[0, 0] > rot_mat[1, 1] and rot_mat[0, 0] > rot_mat[2, 2]:\n        s = 2.0 * np.sqrt(1.0 + rot_mat[0, 0] - rot_mat[1, 1] - rot_mat[2, 2])\n        w = (rot_mat[2, 1] - rot_mat[1, 2]) / s\n        x = 0.25 * s\n        y = (rot_mat[0, 1] + rot_mat[1, 0]) / s\n        z = (rot_mat[0, 2] + rot_mat[2, 0]) / s\n    elif rot_mat[1, 1] > rot_mat[2, 2]:\n        s = 2.0 * np.sqrt(1.0 + rot_mat[1, 1] - rot_mat[0, 0] - rot_mat[2, 2])\n        w = (rot_mat[0, 2] - rot_mat[2, 0]) / s\n        x = (rot_mat[0, 1] + rot_mat[1, 0]) / s\n        y = 0.25 * s\n        z = (rot_mat[1, 2] + rot_mat[2, 1]) / s\n    else:\n        s = 2.0 * np.sqrt(1.0 + rot_mat[2, 2] - rot_mat[0, 0] - rot_mat[1, 1])\n        w = (rot_mat[1, 0] - rot_mat[0, 1]) / s\n        x = (rot_mat[0, 2] + rot_mat[2, 0]) / s\n        y = (rot_mat[1, 2] + rot_mat[2, 1]) / s\n        z = 0.25 * s\n    \n    return np.array([w, x, y, z])\n\n\ndef export_to_fbx_via_blender(motion_data: Dict[str, Any], output_path: Path,\n                               skeleton_type: str = 'core'):\n    \"\"\"\n    Export ARDY motion to FBX using Blender Python API.\n    \n    This function must be called from within Blender's Python environment.\n    Use: blender --background --python converter.py -- --input motion.npz --output motion.fbx\n    \"\"\"\n    if not HAS_BLENDER:\n        raise RuntimeError(\n            \"Blender Python API not available. Run this script with:\\n\"\n            \"blender --background --python converter.py -- --input motion.npz --output motion.fbx\"\n        )\n    \n    import b